<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
<body>
<div class="header-2">
    <nav class="bg-white py-2 md:py-4">
        <div class="container px-4 mx-auto md:flex md:items-center">

            <div class="flex justify-between items-center">
                <a href="<%= '/' %>" class="font-bold text-xl text-indigo-600">ADS PLATFORM</a>
                <button class="border border-solid border-gray-600 px-3 py-1 rounded text-gray-600 opacity-50 hover:opacity-75 md:hidden" id="navbar-toggle">
                    <i class="fas fa-bars"></i>
                </button>
            </div>

            <div class="hidden md:flex flex-col md:flex-row md:ml-auto mt-3 md:mt-0" id="navbar-collapse">
                <a href="<%= dir %>/users/change-username" class="p-2 lg:px-4 md:mx-2 text-indigo-600 text-center border border-solid border-indigo-600 rounded hover:bg-indigo-600 hover:text-white transition-colors duration-300 mt-1 md:mt-0 md:ml-1">CHANGE USERNAME</a>
                <a href="<%= dir %>/users/change-password" class="p-2 lg:px-4 md:mx-2 text-indigo-600 text-center border border-solid border-indigo-600 rounded hover:bg-indigo-600 hover:text-white transition-colors duration-300 mt-1 md:mt-0 md:ml-1">CHANGE PASSWORD</a>
            </div>
        </div>
    </nav>
</div>
<%- include('../partials/flash_message') %>
<section class="text-gray-700 body-font">
    <div class="container mx-auto flex px-5 py-24 md:flex-row flex-col items-center">
        <div class="lg:flex-grow md:w-1/2 lg:pr-24 md:pr-16 flex flex-col md:items-start md:text-left mb-16 md:mb-0 items-center text-center">
            <p class="underline underline-offset-4 title-font sm:text-2xl text-1xl mb-4 font-small text-gray-900">Ads by <%= user.username %></p>
            <% for (let i = 0; i < ads.length; i++) { %>
                path: <%= ads[i].path %> <br>
                <a href="/ads/update/<%= ads[i]._id %>">Edit</a>
                <a href="/ads/delete/<%= ads[i]._id %>">Delete</a><br>
            <% } %>
        </div>
    </div>
</section>
</body>