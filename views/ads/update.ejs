<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
<body>
<%-  include('../partials/header') %>
<%-  include('../partials/flash_message') %>
    <div class="lg:max-w-lg lg:w-full md:w-1/2 w-5/6 container mx-auto mt-16">
        <form class="space-y-5" action="/ads/update/<%= id %>" method="post" enctype="multipart/form-data">
            <%- include('ads_form_partial') %>
            <input name="path" type="file" class="opacity-0" value="<%= ad.path %>" />
                    </label>
                </div>
                <div class="mt-3">
                    <select name="category" class="form-select appearance-none block w-full px-3 py-1.5 text-base font-normal text-gray-700 bg-white bg-clip-padding bg-no-repeat border border-solid border-gray-300 rounded transition ease-in-out m-0 focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-none" aria-label="Default select example">
                        <option selected disabled>Select category</option>
                        <% for (let i = 0; i < categories.length; i++) { %>
                            <% if (categories[i].category == selectedCategory) { %>
                                <option selected value="<%= categories[i]._id %>"><%= categories[i].category%></option>
                            <% } else { %>
                            <option value="<%= categories[i]._id %>"><%= categories[i].category%></option>
                            <% } %>
                            <%=  selectedCategory %>
                        <% } %>
                    </select>
                </div>
                <button class="mt-5 block w-full bg-indigo-500 hover:bg-indigo-600 p-4 rounded text-white transition duration-300 font-medium">Update</button>
            </div>
        </form>
    </div>
</body>
</html>